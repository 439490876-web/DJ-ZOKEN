{
  "name": "newsetki-desktop",
  "private": true,
  "version": "0.1.0",
  "main": "src/main.js",
  "scripts": {
    "dev": "env -u ELECTRON_RUN_AS_NODE ELECTRON_RENDERER_URL=http://localhost:3004 electron .",
    "build": "tsc -p tsconfig.json",
    "start": "env -u ELECTRON_RUN_AS_NODE electron .",
    "test": "node --test"
  },
  "devDependencies": {
    "electron": "^32.3.3",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2"
  }
}
